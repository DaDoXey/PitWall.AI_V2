# ── Python (backend) ──
__pycache__/
*.py[cod]
.venv/
venv/
*.db
.env
.env.*
!.env.example

# ── Node / Next (frontend) ──
node_modules/
.next/
out/
build/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*
next-env.d.ts
*.tsbuildinfo

# ── Env locali del frontend ──
frontend/.env.local
!frontend/.env.local.example

# ── Claude Code tooling (LOCALE: session-cache, hook marker, settings) — mai committare ──
.claude/

# ── Report/sunti di lavoro (materiale di contesto, MAI committati) ──
*_REPORT.md

# ── OS / editor ──
.DS_Store
Thumbs.db
.idea/
.vscode/
