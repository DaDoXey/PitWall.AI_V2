import PageHeader from "@/components/ui/PageHeader";

export default function LoginPage() {
  return (
    <div>
      <PageHeader title="Login" subtitle="ACC GT3" />
      <p className="text-sm text-subtle">
        In costruzione — <span className="text-accent">Fase 6</span>: quick-login demo +
        Google OAuth (NextAuth) quando lo si vorrà.
      </p>
    </div>
  );
}
